import { SystemNamePipe } from './system-name.pipe';

describe('SystemNamePipe', () => {
  it('create an instance', () => {
    const pipe = new SystemNamePipe();
    expect(pipe).toBeTruthy();
  });
});
